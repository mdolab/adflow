   !        Generated by TAPENADE     (INRIA, Tropics team)
   !  Tapenade 3.10 (r5363) -  9 Sep 2014 09:53
   !
   !  Differentiation of setss in forward (tangent) mode (with options i4 dr8 r8):
   !   Plus diff mem management of: s:in si:in sj:in sk:in ss:in-out
   !                ssi:in-out ssj:in-out ssk:in-out
   !
   !      ******************************************************************
   !      *                                                                *
   !      * File:          setss.f90                                       *
   !      * Author:        Eirikur Jonsson                                 *
   !      * Starting date: 10-14-2014                                      *
   !      * Last modified: 10-14-2014                                      *
   !      *                                                                *
   !      ******************************************************************
   SUBROUTINE SETSS_D(nn, ssi, ssid, ssj, ssjd, ssk, sskd, ss, ssd)
   USE BCTYPES
   USE BLOCKPOINTERS_D
   IMPLICIT NONE
   !
   !      Subroutine arguments.
   !
   INTEGER(kind=inttype), INTENT(IN) :: nn
   REAL(kind=realtype), DIMENSION(:, :, :), POINTER :: ssi, ssj, ssk
   REAL(kind=realtype), DIMENSION(:, :, :), POINTER :: ssid, ssjd, sskd
   REAL(kind=realtype), DIMENSION(:, :, :), POINTER :: ss
   REAL(kind=realtype), DIMENSION(:, :, :), POINTER :: ssd
   !
   !      ******************************************************************
   !      *                                                                *
   !      * Begin execution                                                *
   !      *                                                                *
   !      ******************************************************************
   !
   ! Determine the face id on which the subface is located and set
   ! the pointers accordinly.
   SELECT CASE  (bcfaceid(nn)) 
   CASE (imin) 
   ssid => sid(1, :, :, :)
   ssi => si(1, :, :, :)
   ssjd => sjd(2, :, :, :)
   ssj => sj(2, :, :, :)
   sskd => skd(2, :, :, :)
   ssk => sk(2, :, :, :)
   IF (addgridvelocities) THEN
   ssd => sd(2, :, :, :)
   ss => s(2, :, :, :)
   END IF
   CASE (imax) 
   !=======================================================
   ssid => sid(il, :, :, :)
   ssi => si(il, :, :, :)
   ssjd => sjd(il, :, :, :)
   ssj => sj(il, :, :, :)
   sskd => skd(il, :, :, :)
   ssk => sk(il, :, :, :)
   IF (addgridvelocities) THEN
   ssd => sd(il, :, :, :)
   ss => s(il, :, :, :)
   END IF
   CASE (jmin) 
   !=======================================================
   ssid => sjd(:, 1, :, :)
   ssi => sj(:, 1, :, :)
   ssjd => sid(:, 2, :, :)
   ssj => si(:, 2, :, :)
   sskd => skd(:, 2, :, :)
   ssk => sk(:, 2, :, :)
   IF (addgridvelocities) THEN
   ssd => sd(:, 2, :, :)
   ss => s(:, 2, :, :)
   END IF
   CASE (jmax) 
   !=======================================================
   ssid => sjd(:, jl, :, :)
   ssi => sj(:, jl, :, :)
   ssjd => sid(:, jl, :, :)
   ssj => si(:, jl, :, :)
   sskd => skd(:, jl, :, :)
   ssk => sk(:, jl, :, :)
   IF (addgridvelocities) THEN
   ssd => sd(:, jl, :, :)
   ss => s(:, jl, :, :)
   END IF
   CASE (kmin) 
   !=======================================================
   ssid => skd(:, :, 1, :)
   ssi => sk(:, :, 1, :)
   ssjd => sid(:, :, 2, :)
   ssj => si(:, :, 2, :)
   sskd => sjd(:, :, 2, :)
   ssk => sj(:, :, 2, :)
   IF (addgridvelocities) THEN
   ssd => sd(:, :, 2, :)
   ss => s(:, :, 2, :)
   END IF
   CASE (kmax) 
   !=======================================================
   ssid => skd(:, :, kl, :)
   ssi => sk(:, :, kl, :)
   ssjd => sid(:, :, kl, :)
   ssj => si(:, :, kl, :)
   sskd => sjd(:, :, kl, :)
   ssk => sj(:, :, kl, :)
   IF (addgridvelocities) THEN
   ssd => sd(:, :, kl, :)
   ss => s(:, :, kl, :)
   END IF
   END SELECT
   END SUBROUTINE SETSS_D
