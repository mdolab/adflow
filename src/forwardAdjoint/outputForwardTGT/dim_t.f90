   !        Generated by TAPENADE     (INRIA, Tropics team)
   !  Tapenade 3.7 (r4786) - 21 Feb 2013 15:53
   !
   !  Differentiation of dim in forward (tangent) mode (with options debugTangent i4 dr8 r8):
   !   variations   of useful results: dim
   !   with respect to varying inputs: x y
   FUNCTION DIM_T(x, xd, y, yd, dim)
   USE PRECISION
   IMPLICIT NONE
   REAL(kind=realtype) :: x, y, z
   REAL(kind=realtype) :: xd, yd
   REAL(kind=realtype) :: dim
   REAL(kind=realtype) :: dim_t
   EXTERNAL DEBUG_TGT_HERE
   LOGICAL :: DEBUG_TGT_HERE
   IF (.TRUE. .AND. DEBUG_TGT_HERE('entry', .FALSE.)) THEN
   CALL DEBUG_TGT_REAL8('x', x, xd)
   CALL DEBUG_TGT_REAL8('y', y, yd)
   CALL DEBUG_TGT_DISPLAY('entry')
   END IF
   dim_t = xd - yd
   dim = x - y
   IF (dim .LT. 0.0) THEN
   dim = 0.0
   dim_t = 0.0_8
   END IF
   IF (.TRUE. .AND. DEBUG_TGT_HERE('exit', .FALSE.)) THEN
   CALL DEBUG_TGT_REAL8('dim', dim, dim_t)
   CALL DEBUG_TGT_DISPLAY('exit')
   END IF
   END FUNCTION DIM_T
